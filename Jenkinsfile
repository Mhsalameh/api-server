pipeline {
    
    agents any

    steps {

        stage("build") {

            steps {
                echo 'building application ...'
            }
        }

        stage("build") {

            steps {
                echo 'building application ...'
            }
        }

        stage("build") {

            steps {
                echo 'building application ...'
            }
        }
    }
}